This TA has appointments at:
<ul>
  <% @event_list.items.each do |event| %>
    <% start_time = event.start.date_time.to_s.match(/[A-Z](\d)(\d):(\d)(\d)/) %>
    <% @scheduled_times = [start_time.to_s.slice(1..-1)] %>
    <%= @scheduled_times %>
  <% end %>
</ul>

